import React from "react";

const NotFound = (props) => {
    return (
        <li className="not-found">
            <h3>Sorry. No Results Found.</h3>
            <p>Please Try A New Search or Refresh Browser.</p>
        </li>
    );
};

export default NotFound;